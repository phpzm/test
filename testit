#!/usr/bin/php -q
<?php

require_once 'vendor/autoload.php';

use Testit\App;
use Tests\Financial\DocumentType;
use Tests\Financial\StandardHistory;

$app = new App([
    'root' => __DIR__ . '/tests',
    'domain' => 'dev.contasobcontrole.com.br',
    'url' => 'http://dev.contasobcontrole.com.br:9010/',
    'tests' => [
        StandardHistory::class,
        DocumentType::class
    ],
    'cookies' => []
]);

$app->run($argv);